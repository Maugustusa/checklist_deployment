<%= form_with(model: checklist_deployment, local: true) do |form| %>
  <% if checklist_deployment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(checklist_deployment.errors.count, "error") %> prohibited this checklist_deployment from being saved:</h2>

      <ul>
      <% checklist_deployment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :checklist_deployment_title %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :checklist_deployment_description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
